#include <iostream>
#include <string>
using namespace std;

int countWord(string s) {
    return 0;
}
int main() {
    int count = 0;
    string s;
    while (!(cin >> s).eof()) {
        count++;
    }
    cout << count;
    return 0;
}